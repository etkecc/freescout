[global]
; Master captures worker output (below) and writes it here: container stderr.
; Without this the master defaults to a log file and `docker logs` loses PHP errors.
error_log = /proc/self/fd/2
daemonize = no
include = /etc/php83/php-fpm.d/*.conf
